@page
@model LoadKeysModel
@{
    ViewData["Title"] = "Cargar llaves";
}

<form method="post" enctype="multipart/form-data">
    <div class="text-center">
        <h1 class="display-4">Cargar llaves</h1>
        <button type="button" class="btn btn-primary" onclick="document.getElementById('PublicKey').click()">Llave privada</button>
        <input type="file" name="PublicKeyFile" id="PublicKey" style="display: none;" onchange="checkFiles()" />
        <button type="button" class="btn btn-secondary" onclick="document.getElementById('PrivateKey').click()">Llave pública</button>
        <input type="file" name="PrivateKeyFile" id="PrivateKey" style="display: none;" onchange="checkFiles()" /></p>
        <button type="submit" class="btn btn-success" id="loadKeysButton" disabled>Continuar</button>
    </div>
</form>